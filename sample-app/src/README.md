# Sample App - Source

## SampleApp.Web

Minimal ASP.NET Core MVC application created with `dotnet new mvc`.

**Purpose:** Pipeline verification only (not for experiment use)

**Structure:**
- `Program.cs` - Application entry point
- `Controllers/HomeController.cs` - Single controller
- `Views/Home/Index.cshtml` - Single view

**Do not modify** - This is a stable test fixture for validating the Docker evaluation pipeline.
