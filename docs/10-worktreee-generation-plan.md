# Worktree Plan

## Sunday Scope

Tonight:

- create repo docs
- build evaluator
- build scripts/config/docker/structured-context seeds
- stabilize main

Next:

- generate run folders
- create branches/worktrees
- execute runs across the week
- analyze by Wednesday

## Strategy

- branch per run
- worktree per branch

## After main is ready

Generate branches/worktrees.

## Merge Strategy

Merge runs into main after completion.
Keep branches for history.
